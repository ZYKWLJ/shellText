# 命令——只有两个
## find 查找
void execute(char* str){
    先统一将传入命令trim，再判断，如果第一个单词是find，那么执行find命令，具体如下：

    if(如果第一个单词是find或者f，那么跳转到以下逻辑：){
        如果没有第二个单词，那么提示：请输入要查找的单词！
        如果有第二个单词，那么执行find命令，具体如下：
            如果第二个单词是str，那么查找单词str
            如果第二个单词是str%，那么查找以str为前缀的单词
            如果第二个单词是%str，那么查找以str为后缀的单词
            如果第二个单词是%str%，那么查找包含str的单词
        如果有超过第二个单词，那么报错，无相关指令：
        
    }else if(如果第一个单词是exit，那么跳转到以下逻辑：){
        如果仅有一个单词，那么执行exit命令，输出退出程序！
        如果有超过一个单词，那么报错，无相关指令：
    }else{
        其他的任何命令，都报错：
        提示：命令不存在！
        请重新输入！
    }
}

find str 查找单词str
find str% 查找以str为前缀的单词
find %str 查找以str为后缀的单词
find %str% 查找包含str的单词

## exit 退出程序

# 单词集合
{
    A={
        "apple",
        "an",
        "and",
        "are",
        "as",
    },
    B={
        "big",
        "blue",
        "be",
        "been",
    },
    C={
        "cat",
        "can",
        "come",
        "cute",
    },
    D={
        "dog",
        "do",
        "does",
        "dance",
    },
}

## 将每一个以特定字母开头的单词写在一个文件里面！这样共有26个文件！

---
# 需求：
请你实现一个指定函数，查找指定单词集合里面的单词，并且高亮显示！
下面是北京介绍：

## 单词库：
现在我有很多库文件，每一个库文件里面都有许多单词集合，每一个单词集合里面都有许多单词，每一个单词都是一个字符串。
也就是：

### 库文件：
const char ****library = {
    // 思维数组绝了！
    "NO"
    "NO",
    "NO",
    "NO",
    words_4,
    "NO",
    words_6,
    "NULL",
};

### 单词集合：
const char **words_4[27] = {
    a_4,
    b_4,
    "NULL",

};
const char **words_6[27] = {
    a_6,
    b_6,
    "NULL",

};

### 单词：
const char *a_6[] = {
    "a6",
    "an6",
    "and6",
    "NULL",

};
const char *b_6[] = {
    "bin6",
    "bing6",
    "bound6",
    "NULL",

};

等等。

现在我想实现这个功能：
```c
char** search_word(char* str, int library);
```
我传入一个字符串，然后返回一个字符串数组，这个字符串数组里面就是在指定的library库里面的所有的单词。
当library为0时，搜索所有库。否则就搜索特定库！
注意，搜索库时，如果发现了匹配的单词，就高亮显示之：
这是提供的高亮函数：
```c
# include "findAndHighlight.h"
int find_and_highlight(char **s, int s_size, char *t)
{
    int flag=0;
    int first = 1;
    int count = 0;
    for (int i = 0; i < s_size; i++)
    {
        char *p = s[i];
        int found = 0;
        while (*p)
        {
            if (strncmp(p, t, strlen(t)) == 0)
            {
                found = 1;
                break;
            }
            p++;
        }
        if (found)
        {
            // printf("%s%s\n第%d搜索结果:%s\n", BOLD,BLUE,i+1,RESET);
            if (!first)
            {
                // putchar(',');
                // puts("");
            }
            p = s[i];
            while (*p)
            {
                if (strncmp(p, t, strlen(t)) == 0)
                {

                    printf("%s%s%s%s", BOLD, YELLOW, t, RESET);
                    p += strlen(t);
                    flag=1;
                }
                else
                {
                    putchar(*p);
                    p++;
                }
            }
            first = 0;
            printf("\n\n");
        }
    }
    // printf("\n");
    return flag;//判断是否找到！
}
```