# 一、综述
以`多库：单库：单表：多字段：单词`的五级目录来管理数据文件
# 二、详细介绍
## 1.总库：
```c
#include "../../include/words.h"

// 库文件定义
const char ****library[] = {
    NULL, // 代替 "NO"
    NULL, // 代替 "NO"
    NULL, // 代替 "NO"
    NULL, // 代替 "NO"
     (const char****)&words_4,
    NULL, // 代替 "NO"
     (const char****)&words_6,
    NULL // 结束标记
};

```

## 2.单库：
```c
# include "../../include/words_lib/words_4.h"
const char ***words_4[] = {
    a_4,
    NULL,
};
```

```c
#include "../../include/words_lib/words_6.h"
const char ***words_6[] = {
    NULL,

};
```

## 3.单表：
```c

#include "../../../include/words_lib/lib4_3/a_4.h"
const char *a_4[][3] = {
    {"abandon", "/əˈbændən/", "v. 遗弃；离开；放弃；终止；陷入n. 放任，狂热"},
    {"ability", "/əˈbɪləti/", "n. 能力，能耐；才能"},
    NULL
};
```

```c
#include "../../../include/words_lib/lib6_3/a_6.h"
const char *a_6[][3] = {
    {"abandon", "/əˈbændən/", "v. 遗弃；离开；放弃；终止；陷入n. 放任，狂热"},
    {"ability", "/əˈbɪləti/", "n. 能力，能耐；才能"},
   
    {"ax", "/æks/", "v. 削减，删除；用斧修整；解雇；询问n. 斧头；爵士乐器（尤指吉他或萨克斯管）n. (Ax) （德、芬）阿克斯（人名）"},
    {"axis", "/ˈæksɪs/", "n. 轴；轴线；轴心国"},
    NULL
};

```
## 4.单字段：
```c
// 就是一个二维数组，存储了单词、音标、释义
   {"ability", "/əˈbɪləti/", "n. 能力，能耐；才能"},
```

